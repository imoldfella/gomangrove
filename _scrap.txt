type Syllabus struct {
	Title    string
	Image    string // []byte for hash?
	Hash     string
	minGrade int
	maxGrade int
	subject  []string
	Status   int
}

type PinPage struct {
	Title string
	Pin   []Item
	More  []Item
}
func (d *Builder) PinPage(pg PinPage) string {
	var b bytes.Buffer
	d.pinList.Execute(&b, &pg.Pin)
	d.pinList.Execute(&b, &pg.More)
	return d.Page(pg.Title,
		b.String(),
	)
}

type School struct {
	Theme string // this might be a hash or name?
	// pins are now by grade and overall.
	// key could be | separated list?
	// how do I get items from that?
	Pin     map[string][]string
	subject map[string]*Subject
}

// departments are one layer below a grade.
// if there is only one subject in the department, we don't show this list.

// when we upload a syllabus there are two steps
// 1. store the syllabus
// 2. regenerate the school

type SchoolGrade struct {
	// seems like we should try to store the image,title,and hash
	// so we don't have to pull it from the file.

	// each subject can have more than one Syllabus
}
